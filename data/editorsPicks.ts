/**
 * Editor's Picks — Lochitra
 *
 * Curate manually by adding or removing article slugs from this array.
 * The homepage will automatically resolve the full post data (title, summary, date, etc.)
 * from the posts array, so no duplication is needed.
 *
 * INSTRUCTIONS:
 * 1. Find the slug of the article you want to feature (it's the MDX filename without the extension,
 *    e.g. data/blog/my-article.mdx → slug is "my-article").
 * 2. Add the slug string to the array below.
 * 3. The section shows up to 3 articles. The first slug gets the large featured card treatment.
 *
 * Keep the list to 3 slugs for the best layout.
 */

export const EDITORS_PICKS_SLUGS: string[] = [
  'chatgpt-vs-gemini-2026', // Large featured card — newest AI Tools article
  'how-to-start-earning-online-beginners-guide',
  'top-technology-trends-2026',
]

